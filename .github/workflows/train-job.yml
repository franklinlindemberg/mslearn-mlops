name: Run experiment

on:
  workflow_call:
    inputs:
      dataset:
        required: true
        type: string
      environment:
        required: true
        type: string
    secrets:
      AZURE_CREDENTIALS:
        required: true
jobs:
  train:
    runs-on: ubuntu-latest
    environment: ${{inputs.environment}}
    outputs:
      uri_folder: ${{ steps.run.outputs.test }}
    steps:
    - name: Check out repo
      uses: actions/checkout@main
    - name: Install az ml extension
      run: az extension add -n ml -y
    - name: Azure login
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
    - name: Run job
      id: run
      run: echo "test=`$(az ml job create -f infra/job-${{inputs.dataset}}.yml --resource-group "rg-internal" --workspace "mlw-mlops")`" >> "$GITHUB_OUTPUT"
  job2:
    runs-on: ubuntu-latest
    needs: train
    steps:
      - env:
          OUTPUT1: ${{needs.train.outputs.uri_folder}}
        run: echo "da $OUTPUT1"